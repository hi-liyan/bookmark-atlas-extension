# AGENTS.md

本文件用于约束 AI 在本仓库中的编码行为。除非用户明确覆盖，AI 必须遵守以下规则。

## 1. 目标与范围

- 项目目标以 `DESIGN.md` 为准，任务拆解以 `TASKS.md` 为准。
- 平台优先级：Firefox 优先，Chrome 兼容。
- 技术栈固定：React + TypeScript + TailwindCSS + daisyUI + Vite + Zustand。
- 同步方案固定：CouchDB（在设置页可配置与开关）。

## 2. 执行原则

- 先做可运行最小闭环，再做增强，不跳阶段开发。
- 任何实现必须可验证（至少本地构建通过 + 核心路径可手工验证）。
- 不允许“只写示例不落地”的提交；必须交付可运行代码或明确阻塞说明。
- 不允许无依据重构；如非必要，不改变现有对外行为。

## 3. 编码硬约束

- 必须使用 TypeScript 严格类型，禁止滥用 `any`。
- 公共类型放在 `src/shared/types.ts` 或同级共享模块。
- UI 组件优先复用，不重复造轮子；优先使用 daisyUI 组件能力。
- 搜索与同步逻辑必须与 UI 解耦，放在独立 service/module。
- 禁止在组件中直接写复杂业务逻辑（超过 30 行逻辑需抽离）。
- 禁止引入与当前需求无关的新框架或大型依赖。

## 4. 文件与结构约束

- 新增文件必须按既定目录：`popup/`、`options/`、`background/`、`sync/`、`shared/`。
- 不得随意移动或重命名关键入口文件（manifest、background、popup、options）。
- 涉及跨端差异时，优先通过适配层处理，不复制两份业务代码。

## 5. Firefox/Chrome 兼容约束

- API 调用统一使用 `browser.*`（基于 `webextension-polyfill`）。
- manifest 采用双清单策略：`manifest.firefox.json` 与 `manifest.chrome.json`。
- 新增能力时必须说明是否影响双端兼容；若影响，必须给出兼容处理。

## 6. CouchDB 同步约束

- 同步必须支持：`two-way`、`push-only`、`pull-only`。
- 必须实现增量同步（`_changes` + `lastSyncSeq`），禁止每次全量覆盖。
- 必须提供冲突策略：`latest-write-wins`、`prefer-local`、`prefer-remote`。
- 同步失败必须可重试（指数退避）并输出可读错误信息。
- 设置页必须提供：测试连接、立即同步、最近同步状态。

## 7. 安全约束

- 默认使用 HTTPS 连接 CouchDB。
- 不在日志中输出明文密码或 token。
- 凭据存储必须最小化；支持清除凭据。
- 不上传与书签管理无关的数据。
- 涉及删除/批量操作必须二次确认。

## 8. 质量约束

- 每个功能改动至少补齐对应单元测试或说明无法补测原因。
- 搜索、同步、目录操作属于高风险模块，改动后必须有回归验证。
- 提交前至少执行：
  - `pnpm lint`（或等价 lint）
  - `pnpm test`（或最小相关测试）
  - `pnpm build`
- 若环境缺少命令，必须在交付说明中写清未执行项与风险。

## 9. 交付约束

- 输出必须包含：改动文件列表、核心实现点、验证结果、剩余风险。
- 若用户要求“先文档后代码”，不得提前创建业务代码。
- 未经用户明确要求，不做破坏性操作（删除大量文件、重置历史等）。

## 10. 禁止事项

- 禁止伪造测试结果或运行结果。
- 禁止忽略明确报错并宣称“已完成”。
- 禁止为通过编译而注释掉核心逻辑。
- 禁止引入硬编码密钥、账号、外部服务地址到仓库。

## 11. 决策优先级

1. 用户最新明确指令
2. 本文件 `AGENTS.md`
3. `DESIGN.md`
4. `TASKS.md`
5. 默认工程最佳实践

当规则冲突时，按以上顺序执行，并在交付说明中标注冲突与取舍。
